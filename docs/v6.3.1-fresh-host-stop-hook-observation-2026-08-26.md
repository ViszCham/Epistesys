# v6.3.1 Stop hook fresh-host observation — 2026-08-26

- 状態: plugin-bundled Stop hookのfresh-host観測を4ホスト（macos-arm64×2、windows-x86_64×2）で実測。
- Claim boundary: これはテストハーネスがstdinへ直接eventを注入した観測であり、実Codexホストによるplugin pickupではない。promotion、manifest強状態の更新、host enforcementの証明ではない。

## 対象

- hook: `hooks/run-stop-hook.sh`（platform dispatch追加: Darwin/arm64 → `bin/macos-arm64/lc631`、Linux/x86_64 → `bin/linux-x86_64/lc631`、その他 → exit 2 fail-closed）、`hooks/run-stop-hook.ps1`（既存、windows-x86_64）
- packaged binary:
  - `bin/macos-arm64/lc631` sha256 `9f251cd04e19a97b97746f7d6b7357ef81bd611ee84e5f172c6613498380e98d` — 同一ツリーでの `cargo build --release --locked` 出力とSHA一致を実測
  - `bin/windows-x86_64/lc631.exe` sha256 `43c4ce9e08d000ab2fb914409437972ee165003cab148d9634a55419c0e29754` — validation manifestのpinと一致
- test harness: `scripts/test-lc631-host-stop-hook.sh` / `scripts/test-lc631-host-stop-hook.ps1`

## テスト内容（fail-first、各ホスト同一5ケース）

| case | 期待 | 内容 |
|---|---|---|
| T1 | exit 0 | fresh PLUGIN_DATAで正常event → `{"continue":true}`、ledger 1行 |
| T2 | exit 0 | 2件目append → ledger 2行 |
| T3 | 非ゼロ | 不正JSON注入 |
| T4 | 非ゼロ | PLUGIN_DATA/CLAUDE_PLUGIN_DATA未設定 |
| T5 | 非ゼロ | ledger（host-output-v2.jsonl）1バイト改竄後の再実行 |

## 結果

| host | platform | 経路 | 結果 |
|---|---|---|---|
| host A | macOS arm64 (Apple Silicon) | run-stop-hook.sh → macos-arm64 | 5/5 PASS |
| host B | macOS arm64 (Apple Silicon) | run-stop-hook.sh → macos-arm64 | 5/5 PASS |
| host C | Windows x86_64 (RTX 4090) | run-stop-hook.ps1 → windows exe | 5/5 PASS |
| host D | Windows x86_64 (RTX 5060 Ti) | run-stop-hook.ps1 → windows exe | 5/5 PASS |

- 故障注入3種（T3/T4/T5）はすべて exit 2 + stderrに `claim_boundary` 付き構造化エラー。改竄検出（T5）は `stop_hook_replay` エラーで停止し、偽greenにならないことを確認。
- validation manifestのcargoコマンド8本（fmt/check×2/test×2/clippy×2/build --release --locked）はhost Aで全て exit 0。
- `lc631-gpu-doctor --execute` はhost A（Metal経由wgpu）とhost C（RTX 4090）で exit 0、GPU backend `cube_cl_wgpu` が両者で実発火し、CPU/GPU receiptの `context_digest` が両ホスト間で完全一致（`sha256:a1b59506…`）。

## 未実施 / 残余

- Linux x86_64: `bin/linux-x86_64/lc631` は未同梱のため経路は fail-closed（exit 2）のみ確認可能。パッケージ時にLinuxバイナリを追加するまで実観測不可。
- 実Codexホストによる自然なStop hook pickup（simulated stdinでない経路）は未観測のまま。manifestの `fresh_host_v2_receipt_observed` は本記録では更新しない。
- remote CIによる同スイートの実行は未観測（`remote_ci_observed: false` のまま）。
